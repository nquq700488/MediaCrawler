interface:
  display_name: "MediaCrawler"
  short_description: "Repo-specific patterns and workflows for MediaCrawler"
  default_prompt: "Use the MediaCrawler repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true